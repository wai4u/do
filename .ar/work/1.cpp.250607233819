#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	cin>>n;
	vector<int>a(n);
	for(int i=0;i<n;i++)cin>>a[i];
	for(int x=n;x>=0;x--){
		int c=0;
		for(int i=0;i<n;i++)c+=a[i]>=x;
		if(c>=x){
			cout<<x<<'\n';
			return 0;
		}
	}
}
