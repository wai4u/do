#include<bits/stdc++.h>
using namespace std;
int op(int x,int y){return x+y;}
int main(){
	vector<int>a{1,2,3};
	int i=0;
	//a[i++]=op(a[i],1);
	//a[i]=op(a[i++],1);
	a[i]=op(a[++i],1);
	for(int i=0;i<3;i++)cout<<a[i]<<' ';
	cout<<'\n';
}
