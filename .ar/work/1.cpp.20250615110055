#include<bits/stdc++.h>
using namespace std;
void solve(){
	int h,w;
	cin>>h>>w;
	vector<string>t(h);
	for(int i=0;i<h;i++)cin>>t[i];
	if(h>w){
		swap(h,w);
		vector<string>s(h,string(w,'.'));
		for(int i=0;i<h;i++)for(int j=0;j<w;j++)s[i][j]=t[j][i];
		swap(s,t);
	}
	int n=h*w;
	vector<int>cnt(n+n+1),x(w),s(w+1);
	long ans=0;
	for(int u=0;u<h;u++){
		for(int j=0;j<w;j++)x[j]=0;
		for(int d=u;d<h;d++){
			for(int j=0;j<w;j++){
				x[j]+=t[d][j]=='#'?1:-1;
				s[j+1]=s[j]+x[j];
			}
			for(int j=0;j<=w;j++)ans+=cnt[s[j]+h*w]++;
			for(int j=0;j<=w;j++)cnt[s[j]+h*w]=0;
		}
	}
	cout<<ans<<'\n';
}
int main(){
	ios::sync_with_stdio(0),cin.tie(0);
	int t;
	cin>>t;
	while(t--)solve();
}
