#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	cin>>n;
	vector<int>a(n);
	for(int i=0;i<n;i++)cin>>a[i];
	int ng=n+1;
	int ok=0;
	while(ng-ok>1){
		int x=(ok+ng)/2;
		int c=0;
		for(int i=0;i<n;i++)if(a[i]>=x)c++;
		if(c>=x)ok=x;
		else ng=x;
	}
	cout<<ok<<'\n';
}
